// These are the global variables for the project. We can call the variable names anywhere in the project because of the way the HTML is structured

const WIDTH = 1000;
const HEIGHT = WIDTH;
const SQUARE_SIDE = WIDTH / 10;
