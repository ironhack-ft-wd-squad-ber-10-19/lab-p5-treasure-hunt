const WIDTH = 1000;
const HEIGHT = WIDTH;
const SQUARE_SIDE = WIDTH / 10;

const randPositionX = Math.floor((Math.random() * WIDTH) / SQUARE_SIDE) * SQUARE_SIDE;
const randPositionY = Math.floor((Math.random() * WIDTH) / SQUARE_SIDE) * SQUARE_SIDE;
